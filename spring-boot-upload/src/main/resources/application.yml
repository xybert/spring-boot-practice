server:
  port: 9001
  servlet:
    context-path: /springboot/practice/upload

logging:
  level:
    com.xybert.springbootupload: debug

spring:
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB